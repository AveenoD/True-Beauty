-- Phase 1: Run via npm run db:tenant-foundation (Node script handles backfill + this logic).
-- For manual use on existing DBs only when the Node migrator cannot connect.

-- See: backend/scripts/migrate-tenant-foundation.js
